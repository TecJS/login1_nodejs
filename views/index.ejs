<!doctype html>
<html lang="en">
  <%- include('plantillas/head',{title:'Alumnos'}); %>
  <link rel="stylesheet" href="resources/css/style.css">
  <body>
    <!-- Navbar -->
    <% if (login) { %>                
      <% if (rol=='alumno') { %>                
        <%- include('plantillas/navAlumno'); %>
      <% } %>
      <% if (rol=='tutor') { %>                
        <%- include('plantillas/navTutor'); %>
      <% } %>
      <% if (rol=='administrador') { %>                
        <%- include('plantillas/navAdmin'); %>
      <% } %>
  <%}  %>
   
  
     
    
    <div class="container">
      <h1>Página de Inicio</h1>
      <% if (login) { %>                
          <h1>Usuario conectado: <strong><%= name %></strong></h1>
          <h2>Rol: <strong><%= rol %></strong></h2>                        
          <a href="logout" class="a-logout">Logout</a>
      <%} else { %>
          <h1><strong><%= name %></strong></h1>   
          <a href="login" class="a-login">Ir a Login</a>
      <% } %>
    </div>

<!-- Bootstrap Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</body>
</html>
