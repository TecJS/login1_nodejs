<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de tutores sin cuenta</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
</head>
<body>
    <%- include('nav'); %>
    <div class="container">
        <h1 class="my-4">Selecciona tutor para el nuevo grupo</h1>

        <table class="table table-bordered table-striped text-center">
            <thead class="bg-primary text-white">
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Nombre</th>
                    <th scope="col">Correo</th>
                    <th scope="col">Sexo</th>
                    <th scope="col">Acciones</th>
                </tr> 
            </thead>
            <tbody>
                <% results.forEach((alumno) => { %>
                    <tr>
                        <td><%= alumno.id_profesor %></td>
                        <td><%= alumno.nombre_completo %></td>
                        <td><%= alumno.correo %></td>
                        <td><%= alumno.sexo %></td>
                        <td>               
                            <a href="/UsuarioCreate/<%= alumno.id_profesor %>/tutor" class="btn btn-outline-primary">Crear</a>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>
